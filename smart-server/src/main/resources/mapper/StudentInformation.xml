<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jsu.old.mapper.StudentInformationMapper">

    <select id="getStudentInformationDetail" parameterType="com.jsu.dto.QueryDTO" resultType="com.jsu.vo.StudentInformationVO">
        Select * from student_information
        <where>
            <if test="studentNumber != null and studentNumber!=''">student_number = #{studentNumber}</if>
            <if test="studentName != null and studentName!=''">and student_name = #{studentName}</if>
            <if test="classes != null and classes!=''">and classes = #{classes}</if>
            <if test="grade != null and grade!=''">and grade = #{grade}</if>
            <if test="major != null and major!=''">and major = #{major}</if>
            <if test="college != null and college!=''">and college = #{college}</if>
        </where>
    </select>

    <insert id="creatStudentInformation">
         INSERT Ignore INTO student_information (
         student_number, student_name, sex, birthday, native_place, height, id_card,
         family_place, student_phone_number1, student_phone_number2, guardian_ship1,
         guardian_ship2, guardian_phone_number1, guardian_phone_number2, qq_number,
         email, wechat_number, health_status, specialty, hobby, political_status,
         classes, major, grade, college, first_job, second_job, third_job, dormitory_number, photo
         )
         VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.studentNumber}, #{item.studentName}, #{item.sex}, #{item.birthday},
            #{item.nativePlace}, #{item.height}, #{item.idCard}, #{item.familyPlace},
            #{item.studentPhoneNumber1}, #{item.studentPhoneNumber2}, #{item.guardianShip1},
            #{item.guardianShip2}, #{item.guardianPhoneNumber1}, #{item.guardianPhoneNumber2},
            #{item.qqNumber}, #{item.email}, #{item.wechatNumber}, #{item.healthStatus},
            #{item.specialty}, #{item.hobby}, #{item.politicalStatus}, #{item.classes},
            #{item.major}, #{item.grade}, #{item.college}, #{item.firstJob}, #{item.secondJob},
            #{item.thirdJob}, #{item.dormitoryNumber}, #{item.photo}
            )
        </foreach>

    </insert>
</mapper>